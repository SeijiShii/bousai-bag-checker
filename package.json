{
  "name": "bousai-bag-checker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "防災備蓄の鮮度管理 PWA (持ち出し袋チェッカー)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/db/migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "@neondatabase/serverless": "^0.10.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "drizzle-kit": "^0.28.0",
    "@electric-sql/pglite": "^0.2.0",
    "tsx": "^4.19.0"
  }
}
