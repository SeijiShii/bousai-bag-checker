/**
 * 設定画面。Phase A は雛形。Phase C で通知購読フォーム + 投げ銭導線に配線する。
 */
export function SettingsScreen() {
  return (
    <section aria-labelledby="settings-heading">
      <h2 id="settings-heading" className="mb-3 text-lg font-semibold text-text">
        設定
      </h2>
      <p className="text-text-muted">準備中…</p>
    </section>
  );
}
