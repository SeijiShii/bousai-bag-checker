# 依存ライブラリ脆弱性スキャン結果 — 2026-05-28 12:48 (release-pre 再実行)

**スキャン日**: 2026-05-28 12:48 (+09:00)
**対象**: package-lock.json (Node.js / npm)
**スキャナ**: `npm audit --json`
**前回スキャン**: `SECURITY_DEPS_20260528.md` (commit 8e9651e、本日 12:14)
**起動経緯**: `/flow:auto` の P4.7 release-pre 必須監査 再実行。revise 002 retrofit (commit 0b218de) 後に lockfile / deps が変更されていないかを確認。

---

## 1. サマリ

| severity | 件数 | 前回比 | 対応状況 |
|---|---|---|---|
| Critical | 0 | ±0 | - |
| High | 0 | ±0 | - |
| Moderate | 8 | ±0 | **[論点-009] deferred 継続** |
| Low | 0 | ±0 | - |
| **合計** | 8 | ±0 | **新規発生なし** |

**結論**: revise 002 の retrofit は **deps 追加・更新なし** (api file move + response shape 変更 + env rename + test 更新のみ)。package-lock.json は前回スキャン以降未変更で、CVE 結果は SECURITY_DEPS_20260528.md と**完全に同一の 8 件 Moderate** (dev ツールチェーン、[論点-009] deferred)。

## 2. L1 設計レビュー: revise 002 改修への影響

revise 002 で変更された範囲を perspectives O23-O27 で再評価:

| 観点 | 改修ファイル | 評価 |
|---|---|---|
| **O23 認可漏れ** | `api/hub/service-info.ts` + `src/services/serviceInfo/handler.ts` | 共有シークレット認証 (`HUB_SERVICE_INFO_SECRET` 一致時のみ 200、token 不一致 401、env 未設定 503 fail-closed) を維持。SEC-004 認可スコープに変更なし。**finding なし**。 |
| **O24 入力検証** | (該当なし) | service-info は GET のみで request body 受け取らず、URL params 無し。新規入力経路なし。**finding なし**。 |
| **O25 秘密情報** | `.env.example` / `.env.local` rename + `api/hub/service-info.ts` の env 参照 | `HUB_SERVICE_INFO_SECRET` を新規 var として `.env.example` に登録、`.env.local` は gitignored (`.env*.local` カバー)。`process.env.HUB_SERVICE_INFO_SECRET` の参照のみで全文表示・ログ出力なし。**finding なし**。 |
| **O26 PII ログ漏洩** | `src/services/serviceInfo/collectMetrics.ts` (response shape 変更) | response は集計値のみ (`schemaVersion` / `service` / `status` / `metrics[]{mau, users_total, error_count_24h}` / `version` / `extra.generated_at`)。clerk_user_id / users.id 等の PII 一切含めず。test U-P1 で `clerk` / `@` 文字列の非混入を assert 済。**finding なし**。 |
| **O27 レート制限** | `api/hub/service-info.ts` | `InMemoryRateLimiter(60, 60_000)` で IP 単位 60 req/min を維持 (旧 endpoint と同等)。release で Upstash 注入可。**finding なし**。 |
| **O28 依存 CVE** | package-lock.json | 変更なし → 前回スキャンと完全同一。 |

→ **revise 002 retrofit による新規 SEC finding ゼロ**。既存 SECURITY_REVIEW_20260526.md / SECURITY_DEPS_20260528.md の結論を維持。

## 3. 過去 findings 取り崩し (Step 6.5)

§8 SEC findings (concept §8 [論点-004]〜[論点-009]) の status を確認:

| 論点 | severity | status | アクション |
|---|---|---|---|
| 論点-004 [SEC-001] O23 認可 | High | accepted-as-requirement | concept §3.1 NFR 組み込み済、各 feature 実装で対応済 (withOwner 等) |
| 論点-005 [SEC-002] O26 PII | High (legal_required) | accepted-as-requirement | _shared/notification 実装で scrubPII/sentryBeforeSend 完了 |
| 論点-006 [SEC-003] O24 入力検証 | Medium | dispatched-to-feature | 各 feature SPEC + 実装で Zod + CSV escape 完了 |
| 論点-007 [SEC-004] O27 レート制限 | Medium | dispatched-to-feature | feedback/tip/service-info 共通レート制限 (`src/services/ratelimit`) で完了 |
| 論点-008 [SEC-005] drizzle-orm SQLi | High | closed | session 40 で 0.36.4→0.45.2 アップグレード解消 |
| 論点-009 [SEC-006] dev ツール CVE | Medium | open (deferred) | Dependabot 継続監視に委譲、SemVer major 移行はリリース後 |

→ **取り崩し対象なし**。全 finding が status 確定済 + 対応経路明示済。

## 4. 結論

release-pre 必須監査 (audit + secure) 再実行ともに **fresh + 新規 finding ゼロ**。

- audit (AUDIT_20260528_1246): Critical 0 / High 0 / Medium 0 / drift 解消
- secure (本回): Critical 0 / High 0 / Moderate 8 (既知 deferred) / L1 revise 002 影響なし / 過去 findings 全 status 確定済

P4.7 Release gate 評価に合流可能。
