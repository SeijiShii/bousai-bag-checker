# AI_LOG セッション D20260527_045 — /flow:e2e _shared/i18n

**実行日時**: 2026-05-27 18:35 (+09:00)
**コマンド**: /flow:e2e _shared/i18n (ローカル headless, Class A)
**状態**: 完了 (E2E green)
**含まれる decision**: D20260527-106 〜 D20260527-107
**起動元**: P4.5 E2E gate (i18n 101 完了 + 004 存在 + 103 不在)
**depends_on**: D20260527-104 (i18n 実装)
**Resume Contract 準拠**

---

## 主要決定サマリ
- 言語切替ジャーニー S1-S7 の E2E spec (`e2e/i18n.spec.ts`) を実装 → green
- **E2E で実装バグを発見・即修正**: `<html lang>` が初期検出/リロード時に未同期 (setLocale でしか syncHtmlLang を呼んでいなかった) → S5(検出)/S2(リロード) RED → config に `i18n.on('languageChanged', applyHtmlLang)` + 初期適用を追加して GREEN
- 既存 spec の i18n 追従: playwright.config に `locale: 'ja-JP'` 固定、inventory/shopping の変更文字列追従、LanguageSwitcher に data-testid 追加
- **全体 E2E 18 passed** (i18n 7 + 既存 11)、flaky なし。unit 14 再確認 green

## 生成・更新ファイル
- `e2e/i18n.spec.ts` (新規) / `e2e/inventory.spec.ts` `e2e/shopping.spec.ts` (文字列追従) / `playwright.config.ts` (locale)
- `src/i18n/config.ts` (html lang 同期バグ修正) / `src/components/ui/language-switcher.tsx` (data-testid)
- `103__shared_i18n_E2E_REPORT.md` + i18n INDEX + AI_LOG INDEX

## 次のステップ
- **P4.45 Wording gate (再評価)**: i18n で UI/コピーが変わった + JA カタログが新ソース → `/flow:wording` を catalog に対して提示 (Class C 人間校正)。元々ユーザーが着手していた wording を i18n 完成後に catalog ベースで実施
- P4.7 Release gate (実キー + デプロイ、Class C/B) は引き続き保留

---

## decisions

### D20260527-106
- question: E2E で発見した html lang 未同期バグの扱い
- chosen: fix seed でなく本セッションで即修正 (config に languageChanged リスナー + 初期適用)。i18n feature 自体の未リリースバグのため
- chosen_type: auto-recommended
- context: S5(検出)/S2(リロード) が RED。syncHtmlLang が setLocale 限定で init/検出時に呼ばれず。一元化で 18 E2E green
- depends_on: [D20260527-104]

### D20260527-107
- question: 既存 E2E の i18n ロケール安定化
- chosen: playwright.config に locale='ja-JP' 固定 + 変更文字列追従 + switcher data-testid
- chosen_type: auto-recommended
- context: E2E ブラウザの navigator 依存で既定ロケールが揺れると既存 JP assert が壊れる。ja 固定で安定化、i18n spec は newContext/selectOption で個別ロケール検証
- depends_on: [D20260527-106]
